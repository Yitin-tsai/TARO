steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'taro'
      - '--source=.'
      - '--region=asia-east1'

options:
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET
